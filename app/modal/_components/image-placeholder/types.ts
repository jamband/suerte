export type Props = {
  className?: string;
  text: string;
  textSize: `${string}em` | `${string}rem`;
  textFill: `#${string}`;
  rectFill: `#${string}`;
  height: number;
};

export type _Props = Props;
