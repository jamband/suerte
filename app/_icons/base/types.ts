export type Props = {
  className?: string;
  style?: React.CSSProperties;
  viewBox: `${number} ${number} ${number} ${number}`;
  children: React.ReactNode;
};

export type _Props = {
  className?: string;
  style?: React.CSSProperties;
};
